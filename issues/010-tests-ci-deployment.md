# Add tests, CI, and deployment config

Add unit and integration tests, a GitHub Actions CI pipeline, and simple deployment artifacts (Dockerfile and docker-compose). Include instructions for local development and a basic production deployment guide.

Acceptance criteria:
- Test suite with core unit/integration tests.
- GitHub Actions workflow runs tests on push/PR.
- Dockerfile and docker-compose for local dev and quick deploy.
