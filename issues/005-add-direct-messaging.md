# Add direct messaging between users

Implement private messaging API and a simple UI for user-to-user messages and small group chats. Persist messages and add unread counts.

Acceptance criteria:
- Message model and endpoints (send/list/mark-read).
- Simple frontend inbox view and message thread UI.
- Unread message counters for recipients.
